# Example Blog Output

This file is a placeholder for an example blog generated by the pipeline.

Run the project with:

```bash
python main.py "Your topic here" -o examples/example_blog.md
```

Or use the Streamlit demo:

```bash
streamlit run app.py
```

Then generate a blog and download the Markdown—you can save it here as a reference.
